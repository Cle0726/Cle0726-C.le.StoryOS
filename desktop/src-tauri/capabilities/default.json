{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Minimal StoryOS desktop capability. No shell, filesystem, network or mutation plugin permissions.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
